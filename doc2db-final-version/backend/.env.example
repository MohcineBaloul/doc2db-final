# Doc2DB-Gen – copy this file to .env and set your key
# Required for LLM extraction (vision + schema)
OPENAI_API_KEY=sk-your-key-here

# Optional – default is SQLite in backend folder
# SQLite (default):
# DATABASE_URL=sqlite+aiosqlite:///./doc2db.db
# PostgreSQL (e.g. with Docker: docker compose up -d):
DATABASE_URL=postgresql+asyncpg://doc2db:doc2db_secret@localhost:5432/doc2db
MAX_UPLOAD_MB=20
